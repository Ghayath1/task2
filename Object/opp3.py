#(1)->A base class is an existing class from which the other classes are derived# and inherit the methods and properties.
#(2)->Parent class is the class being inherited from, also called base class. Ch  #ild class is the class that inherits from another class.
#(3)->Subclasses,also referred to as derived classes, heir classes, or child cl#    #asses, are classes that inherit one or more language entities from another     class
#(5)->A class can be derived from more than one classes, which means it can inhe#rit data and functions from multiple base classes.
#(6)->instance is an object that belongs to a class.
#(2)->Polymorphism allows us to access these overwritten methods and attributes that have the same name as the parent class.
#(3)->yes.A class can be derived from more than one base class in Python, this is called multiple inheritance.
#In multiple inheritance, the properties of all base classes are inherited into the derived class. The syntax for multiple inheritance is similar to simple inheritance
